export const enum State {
  IDLE = 'idle',
  MOVE_LEFT = 'move-left',
  MOVE_RIGHT = 'move-right',
  MOVE_UP = 'move-up',
  MOVE_DOWN = 'move-down',
}

export const enum StateSprite {
  IDLE = 'idle',
  MOVE_HORIZONTAL = 'run',
  MOVE_VERTICAL = 'climb',
}
